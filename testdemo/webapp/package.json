{
  "name": "mta-project",
  "version": "0.0.1",
  "description": "Build and deployment scripts",
  "scripts": {
    "clean": "rimraf resources mta_archives mta-op*",
    "build": "rimraf resources mta_archives && mbt build --mtar testdemo",
    "deploy": "cf deploy mta_archives/testdemo.mtar --retries 1",
    "undeploy": "cf undeploy testdemo --delete-services --delete-service-keys --delete-service-brokers"
  },
  "devDependencies": {
    "mbt": "^1.2.29",
    "rimraf": "^5.0.5"
  }
}
